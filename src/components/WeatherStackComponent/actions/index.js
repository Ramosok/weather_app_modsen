import { createAction } from "redux-actions";

export const GET_WEATHER_ALTERNATIVE_REQUEST = createAction(
  "GET_WEATHER_ALTERNATIVE_REQUEST"
);
export const GET_WEATHER_ALTERNATIVE_SUCCESS = createAction(
  "GET_WEATHER_ALTERNATIVE_SUCCESS"
);
export const GET_WEATHER_ALTERNATIVE_FAIL = createAction(
  "GET_WEATHER_ALTERNATIVE_FAIL"
);
